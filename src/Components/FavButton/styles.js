import styled from 'styled-components'

export const Button = styled.button`
    display: flex; 
    align-items: center;
    padding-top: 8px; 
    background-color: transparent;
    border: 0;   
    & svg {
        margin-right: 4px;
    }
`
